import React from 'react';

type ImageViewProps = {
  // Define the props for the ImageView component
};

const  ImageView: React.FC<ImageViewProps> = () => {
  return (
    <div>
      <h1>Image View </h1>
    </div>
  );
}

export default ImageView;