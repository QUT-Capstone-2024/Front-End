import React from 'react';

type NotFoundPageProps = {
  // Define the props for the NotFoundPage component
};

const NotFoundPage: React.FC<NotFoundPageProps> = () => {
  return (
    <div>
      <h1>Page not found</h1>
    </div>
  );
};

export default NotFoundPage;
